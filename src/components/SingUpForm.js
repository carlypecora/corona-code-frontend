import React, { useState } from "react";
import styled, { css } from "styled-components";

export default function SignInForm() {
  return (
    <>
      <Wrapper>I will be a Sign In form</Wrapper>
    </>
  );
}

const Wrapper = styled.div`
  display: flex;
  flex-direction: row;
`;
