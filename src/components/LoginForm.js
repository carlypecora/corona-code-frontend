import React, { useState } from "react";
import styled, { css } from "styled-components";

export default function LoginForm() {
  return (
    <>
      <Wrapper>I will be a log in form</Wrapper>
    </>
  );
}

const Wrapper = styled.div`
  display: flex;
  flex-direction: row;
`;
